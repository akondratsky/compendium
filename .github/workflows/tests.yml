name: 'Run tests'

on:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: yarn install
        run: yarn install

      - name: Run unit tests
        run: yarn test
