name: 'Test'
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: yarn install
        run: yarn install
      - name: Build
        run: yarn test
